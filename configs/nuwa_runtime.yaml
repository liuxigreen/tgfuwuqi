default_nuwa_version: nuwa_default_v1
min_confidence_for_demo_auto: 0.65

fast_path:
  enabled: true
  timeout_ms: 800
  min_confidence_for_demo_auto: 0.65
  fallback_to_conservative: true

deep_path:
  enabled: true
  run_in_fast_path: false
  output_proposed_changes_only: true

versions:
  nuwa_default_v1:
    skill_path: distillation-data/Traders/xiaomustock/SKILL.md
    enabled: true
    mode: mock_structured
